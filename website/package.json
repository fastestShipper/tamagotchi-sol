{
  "name": "brokedev-games-studio-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Public teaser and studio site for BROKEDEV.GAMES.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --check app.js && node --check experience.js && node --check arcade.js && node --check scripts/build.mjs",
    "preview": "python -m http.server 8130 --bind 127.0.0.1 --directory dist"
  },
  "dependencies": {
    "three": "0.185.1"
  }
}
